apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-deploy
  namespace: metacoding
spec:
  replicas: 2
  selector:
    matchLabels:
      app: delivery
  template:
    metadata:
      labels:
        app: delivery
    spec:
      containers:
        - name: delivery-server
          image: metacoding/delivery:1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8084
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
          envFrom:
            - configMapRef:
                name: delivery-configmap
            - secretRef:
                name: delivery-secret