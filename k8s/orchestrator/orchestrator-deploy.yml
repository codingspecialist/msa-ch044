apiVersion: apps/v1
kind: Deployment
metadata:
  name: orchestrator-deploy
  namespace: metacoding
spec:
  replicas: 2
  selector:
    matchLabels:
      app: orchestrator
  template:
    metadata:
      labels:
        app: orchestrator
    spec:
      containers:
        - name: orchestrator-server
          image: metacoding/orchestrator:1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
          envFrom:
            - configMapRef:
                name: orchestrator-configmap

